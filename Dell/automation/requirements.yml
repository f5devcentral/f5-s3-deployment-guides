---
collections:
  - name: f5networks.f5_modules
    version: ">=1.27.0"
  - name: ansible.netcommon
    version: ">=5.0.0"
  - name: community.general
    version: ">=5.0.0"
  # Self-signed certificate generation (bigip_ssl_certs role).
  - name: community.crypto
    version: ">=2.10.0"
